name: Browser capture acceptance
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  capture:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    env:
      AEGIS_BROWSER_CHANNEL: chrome
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        if: runner.os != 'Windows'
        with:
          python-version: '3.14'
      - name: Prepare and select USB Python on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          ./scripts/prepare_windows_runtime.ps1
          "AEGIS_TEST_PYTHON=runtime/python/python.exe" >> $env:GITHUB_ENV
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run test:capture
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: capture-failure-${{ matrix.os }}
          path: test-results/
          retention-days: 7
